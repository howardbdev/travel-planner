<div class="row" id="logged_in_background" >

    <div class="col-sm col-md-10 offset-md-4 big_block" >


      Hello, <%=@user.first_name%>!
      <br></br>
      <%if @user.trips.empty?%>
        You have no existing trips, to create one, click <a href="/trips/new">here</a>
      <%else%>

        Your trips are:

        <section class="col-sm-8 table-responsive">
          <table class="table table-bordered table-striped table-condensed">
            <thead>
              <tr> <th>Origin</th> <th>Destination</th><th>Departing</th><th>Returning</th><th>Transportation</th>   </tr>
            </thead>
            <tbody>
              <%@user.trips.each do |trip|%>
              <tr>
                <td><%=trip.origin%></td>
                <td><%=trip.destination%></td>
                <td><%=trip.departing%></td>
                <td><%=trip.returning%></td>
                <td><%=trip.transportation%></td>
                <td>
                  <form method="get" action="/trips/<%=trip.slug%>/edit">
                    <input type="submit" value="Edit Trip">
                  </form>
                </td>
                <td>
                  <form action="/trips/<%=trip.slug%>" method="POST">
                    <input type="hidden" id="hidden" name="_method" value="DELETE">
                    <input type="submit" value="Delete Trip">

                  </form>
                </td>
              </tr>
              <%end%>

            </tbody>
            </table>
        </section>
      <%end%>

    </div>
  </div>
</div>
